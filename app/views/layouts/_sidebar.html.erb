<div class="d-flex col-sm-3 col-md-2 sidebar">
  <ul>
    <li>Menu</li>
    <% @categories.each do |category| %>
      <li>
        <%= link_to category.name, category_path(category) %>
      </li>
    <% end %>
  </ul>
</div>
